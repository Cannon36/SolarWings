using UnityEngine;
using System.Collections;

public class Level3 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Physics.gravity = new Vector3 (0f, -6f, 0);//Just change the gravity
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
