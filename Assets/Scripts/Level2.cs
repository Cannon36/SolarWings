using UnityEngine;
using System.Collections;

public class Level2 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Physics.gravity = new Vector3 (0f, -2f, 0f);//Just change the gravity less
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
